schema_version: '2.0'
change_id: none
status: no-change-in-progress
note: >-
  GH-97-right-sizing is built, tested and pushed to main (b3c3a2c). Retired here so
  the 2.9.0 release can run under its own release-workflow change, which is what
  the publish gate requires: publishing under a development change is how a
  release skips its own review.
