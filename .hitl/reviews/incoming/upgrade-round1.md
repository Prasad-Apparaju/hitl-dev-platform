# Upgrade lens, round 1: HITL 2.13.0

Reviewed: source `hitl-dev-platform` at `b5b1bed` (v2.12.1 → 2.13.0), plugin repo `release/2.x` at `efdad0c` plus the uncommitted `scripts/build.sh` edit. Build made in a scratch copy of the plugin repo (`<scratch>/plugin`, below "build"). Nothing in either repo was modified; no GitHub writes; no `--confirmed` anywhere.

| # | Check | Command (in the build unless noted) | Result | Output that decided it |
|---|---|---|---|---|
| 1 | Build | `cp -R hitl-claude-plugin <scratch>/plugin; bash scripts/build.sh /Users/Prasad_1/Projects/hitl-dev-platform` | pass | exit 0, `Build complete.`; `plugin.json` `"version": "2.13.0"`; `grep -c 'SOURCE PATH'` = 0. `git status --short` = 30 entries: 27 M (plugin.json, CHANGELOG.md, hooks/_steps.sh, first-pass-permissions.sh, statusline-hitl.sh, scripts/build.sh, shared/challenge-stance.md, shared/ci/first-pass/{check_skips,size_plan}.py, shared/ci/{retired-tests,shipped-validators}.sha256, shared/getting-started.md, shared/templates/claude-md-hitl-block.md, skills/{dev-apply-change,dev-generate-docs,dev-retro,dev-start-brownfield,dev-start-change,dev-start-from-prd,dev-start-migration,dev-update,help,ops-migrate-database,ops-post-deploy-monitor,qa-plan-tests,qa-review-tests,qa-verify-quality}/SKILL.md) + 3 ?? (shared/tools/hitl-onboarding/release_notice.py, skills/dev-start-migration/behavioral-inventory-template.md, skills/dev-update/legacy-statusline.md) |
| 2 | shared/ refs resolve | `grep -rhoE 'shared/[A-Za-z0-9_./-]+' skills agents commands \| sed 's/[.,;:)`]*$//' \| sort -u` then `test -e` each | pass | 56 unique refs, 0 missing. `release_notice.py`, `size_plan.py`, `check_skips.py` all present |
| 3 | skill-file refs resolve | `grep -rhoE 'skills/[a-z0-9-]+/SKILL\.md' skills \| sort -u` then `test -e` | pass | 7 unique refs, 0 missing |
| 4 | Siblings ship | `test -e` + `grep -c <basename> <skill>/SKILL.md` | pass | legacy-statusline.md, behavioral-inventory-template.md, observability-survey.md present; each named once in its SKILL.md |
| 5 | Manifest current | `diff shared/ci/shipped-validators.sha256 <source>/ci/shipped-validators.sha256`; `shasum -a 256` of built check_skips.py / size_plan.py; every `ci/` path in manifest: built copy's hash present | pass with one gap | diff clean. check_skips `b77baaa9…` and size_plan `e178bee1…` both listed `# 2.13.0`. Every `.py` built hash is listed. Gap: built `shared/workflows.yaml` (installs as `ci/first-pass/workflows.yaml`) hashes `191d6d02…`, which appears on NO manifest line; the `# 2.12.1` line carries the source hash `26322286…` (build.sh rewrites one comment path, so built ≠ source). Older lines (2.10.0 `a71f240b…`, 2.11.0 `9a6533e9…`) DO carry built hashes |
| 6 | Upgrade from 2.12.1 | scratch repo with 2.12.1 `check_skips.py`, `size_plan.py`, `resurface.py`+1 comment line; `python3 <build>/shared/ci/first-pass/migrate_project.py --root . --sync-validators <build> --apply` | pass | `^ updated ci/first-pass/check_skips.py (an older shipped version, unmodified)`, same for size_plan.py; `cmp` equal to built copies; `~ ci/first-pass/resurface.py differs from the shipped version — KEPT yours`, still ends `# repo edit for review`; exit 0. Re-run with the 2.12.1 built `workflows.yaml` added: `1 identical` (2.12.1 and 2.13.0 builds of it are byte-identical). With 2.11.0 / 2.10.0 built workflows.yaml: `^ updated … (an older shipped version, unmodified)` |
| 7 | In-flight change not newly blocked | `git show v2.12.1:ai/shared/templates/impact-record.schema.yaml`; `git show v2.12.1:ai/claude/apply-change/SKILL.md` Step 3; `check_skips.py --workflows <build>/shared/workflows.yaml` on `GH-000-example.yaml` (has change_id + workflow, no impact_record) | pass | schema lines 21 `change_id:` ("must match … mismatch is a blocking error") and 26 `workflow:` (enum); skill line 87-88 "Write the record to .hitl/impact/<change_id>.yaml, against ai/shared/templates/impact-record.schema.yaml". Run: `First Pass skip ledger: clean.` exit 0, neither code fired. Read: a 2.12.1 record that omitted `workflow` gets `RECORD_UNIDENTIFIED` (non-waivable, check_skips.py:482) with the message naming the key and value to add; the change file itself is not touched |
| 8 | dev-update Step 4.10 | block extracted from `skills/dev-update/SKILL.md` (3 lines), run with `CLAUDE_PLUGIN_ROOT=<build> HITL_HOME=$(mktemp -d)` in a scratch repo | pass | first line `ask`, then the two question texts (state output), exit 0; `grep -c confirmed` = 0; isolated home has 0 files |
| 9 | Brownfield Step 11 degrades | block extracted (lines 29-31 of Step 11), run with `HOME=$(mktemp -d)` | pass | `release_notice.py is not in this build: skipping.` exit 0, no traceback, home empty |
| 10 | Fresh-install sanity | `claude plugin validate <build>`; `find <build> -name '*.md' -path '*skills*' \| wc -l`; SKILL.md counts | pass | `✔ Validation passed`. 71 .md under skills; 58 SKILL.md in build; 58 SKILL.md under source `ai/claude/` (names differ only by the build's `pm/x → pm-x` flattening; none missing) |
| 11 | Notes vs files | `grep release-notice.yaml release_notice.py`; `grep proposed size_plan.py` | pass | `USER_FILE_NAME = "release-notice.yaml"` under `$HITL_HOME` default `~/.hitl/` (line 10, 45). `"proposed": proposed(outcomes, option)` emitted at line 316 |

## Points

1. **Worth deciding.** The shipped-validators manifest has no line for the built `workflows.yaml` (hash `191d6d02…`, the file 2.12.1 and 2.13.0 actually install). The 2.12.1 line lists the source hash, which no repo ever holds. Harmless for this upgrade (both builds are identical, migrator reports `identical`), but the next release that changes `workflows.yaml` will report every 2.12.1/2.13.0 repo's copy as "modified here, kept": the plugin #35 pattern the manifest exists to prevent. `ci/wiring/test_shipped_validators_manifest.py` hashes the source tree, so it cannot see this.
2. **Minor.** On the `ask` path, Step 4.10's `state` prints the verdict line and then the full text of both questions (observed; other paths not run). Fine for the model reading it; more than the "one verdict line" the checklist describes.

Everything else checked is right.

VERIFIED
