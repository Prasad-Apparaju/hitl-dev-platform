---
description: QA review — verify test coverage against acceptance criteria, LLD edge cases, and incident registry regressions
argument-hint: "[feature name or PR link]"
---

Use the `qa-reviewer` agent to review test coverage for $ARGUMENTS.

Do not approve until all acceptance criteria are covered, all LLD error modes are tested, incident regressions from the registry are present, and the test registry is updated.
