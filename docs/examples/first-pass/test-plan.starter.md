# Test plan (starter) — refund-flow

One happy-path case per component:
- refund_service: a valid refund request succeeds and records the refund.

needs-enhancement: edge cases (partial refund, double-submit), failure modes, negative tests.
